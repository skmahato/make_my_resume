const styles = () => ({
  root: { display: 'flex' },

  drawerHeader: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    minHeight: 60
  },

  content: {
    flexGrow: 1,
    marginBottom: 30
  }
});

export default styles;
