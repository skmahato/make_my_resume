const styles = () => ({
  root: { display: 'flex' },

  drawerHeader: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    minHeight: 60
  },

  content: {
    flexGrow: 1,
    marginBottom: 30,
    padding: '15px 45px 15px 45px'
  }
});

export default styles;
